> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
> 1. outer
>    - inner
>      ```rust
>      let nested = true;
>      ```

| a | b |
|---|---|
| 1 | 2 |

[^n]: nested footnote
xx